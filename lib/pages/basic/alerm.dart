import 'package:flutter/material.dart';

class Alerm extends StatelessWidget {
  const Alerm({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Alerm Page')),
      body: Center(child: Text('Welcome to Alerm Page')),
    );
  }
}
